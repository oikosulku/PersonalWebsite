<% if( success && success.length ) { %>
    <div class="alert">
        <%=success%>
    </div>
<% } %>
    
<% if( error && error.length ) { %>
    <div class="alert">
        <%=error%>
    </div>
<% } %>